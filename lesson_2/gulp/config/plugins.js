import replace from "gulp-replace"; // Пошук і заміна
import browsersync from "browser-sync";
//  Об'єкт який експортується
export const plugins = {
    replace: replace,
    browsersync: browsersync
};